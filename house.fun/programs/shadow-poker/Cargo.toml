[workspace]
members = [
    "programs/*"
]
resolver = "2"

[profile.release]
overflow-checks = true

[patch.crates-io]
# indexmap >=2.7.0 requires rustc 1.82, but cargo build-sbf uses the
# Solana-bundled rustc 1.79.0-dev. Pin to last compatible version.
indexmap = "=2.6.0"
